services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./app:/app
    environment:
      - npm_config_cache=/app/.npm
    ports:
      - "8080:8080"
    command: make dev
